@page
@model NET_Projekt.Pages.Categories.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Kategorie</h1>

<p>
    <a class="btn btn-info" asp-page="Create">Nowa Kategoria</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Category[0].Name)
            </th>
            <th>
                Opcje
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Category) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @*<a asp-page="../CategoryRecipes/Create" asp-route-id="@item.Id" asp-route-name="@item.Name">Add To Category</a> |
                <a asp-page="../CategoryRecipes/Index" asp-route-id="@item.Id" >Delete From Category</a> |*@
                <a asp-page="./Edit" asp-route-id="@item.Id">Edytuj</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Szczegóły</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Usuń Kategorię</a>
            </td>
        </tr>
}
    </tbody>
</table>
