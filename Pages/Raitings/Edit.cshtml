@page
@model NET_Projekt.Pages.Raitings.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Raiting</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Raiting.IsPositive" class="control-label"></label>
                <input asp-for="Raiting.IsPositive" class="form-control" />
                <span asp-validation-for="Raiting.IsPositive" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="Raiting.ApplicationUserID" />
            <input type="hidden" asp-for="Raiting.RecipeID" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
